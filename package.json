{
  "name": "localtube-saver-root",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "description": "LocalTube Saver monorepo root",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  },
  "lint-staged": {
    "apps/desktop/**/*.{ts,js}": [
      "npm run lint -w localtube-desktop"
    ]
  },
  "scripts": {
    "prepare": "node -e \"if (process.env.NODE_ENV !== 'production' && process.env.CI !== 'true') { try { (require('husky').default || require('husky'))() } catch (e) { try { require('child_process').execSync('husky', {stdio: 'inherit'}) } catch (e2) {} } }\""
  }
}
